# Node modules (dependencies)
node_modules/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-specific
.DS_Store
Thumbs.db

# Environment & secrets
.env
.env.local
.api_key

# Build & cache
dist/
build/
.cache/
tmp/
temp/

# Puppeteer Chromium downloads
.puppeteer/

# VSCode settings (optional, kalau pakai VSCode)
.vscode/

# Dependency lock file (optional, kalau hanya pakai yarn/npm saja)
package-lock.json
yarn.lock

# WhatsApp session & cache (data sensitif)
_IGNORE_*/
_session_*/
.wwebjs_cache/
.wwebjs_auth/
*.data.json

# Runtime data (dibuat otomatis)
accounts.json
chat-inbox.json
chat-sent.json
hasil/
hasil.json
hasil.xlsx
hasil.txt
numbers.json

# Shortcut Git folder di project root (tidak perlu di repo)
Git/
